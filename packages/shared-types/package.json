{
  "name": "@wapp/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical enums, DTO interfaces and permission model shared between apps/api, apps/web and apps/admin. Single source of truth — no business enum is ever redefined per-app.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@wapp/config": "workspace:*",
    "eslint": "^9.14.0",
    "typescript": "^5.6.3"
  }
}
