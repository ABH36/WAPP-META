export * from "./lib/cn";
export * from "./lib/status-color";
export * from "./components/button";

// Additional components (DS-001 §4 full inventory: Input, Select, Table, Card,
// Badge, Modal, Toast, etc.) are added incrementally, one per module's frontend
// implementation step (SDP-001 §6 "Module Development Order", step 9 of the
// 12-step module lifecycle) — not all at once in Phase 1 Foundation. Button is
// the reference pattern every subsequent component follows.
